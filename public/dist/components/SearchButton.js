import React from 'react';
import './SearchButton.css';
export const SearchButton = ({ onClick }) => {
    return (React.createElement("button", { className: "search-button", onClick: onClick, "aria-label": "Search all artists and songs" }, "SEARCH ALL ARTISTS & SONGS"));
};
//# sourceMappingURL=SearchButton.js.map